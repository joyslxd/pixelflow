# Context Dictionary

调查结论沉淀库。**调查代码前先检索：**

```bash
rg "<关键词>" .spec/context-dict/ -l
```

有结论直接复用；满足触发条件再写入（见 `ai-coding-context/conventions.md`）。

当前域：

- `video-agent/` — VideoAgent 入口路径、确认闸门、资产包、计划卡、额度恢复等
