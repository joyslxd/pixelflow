---
topic: V2.1 批次 E 隔离未接线 V1 video workflow
module: video-agent
date: 2026-08-11
keywords:
  - agent_workflows/video
  - v1_workflow_legacy
  - Batch E
  - quarantine
  - frontend_v2 Job
---

## 结论摘要

批次 E 本轮只做**隔离**，不做硬删：`pixelflow.agent_workflows.video` 已无 Gateway / `video_agent` / `agent_runtime` 生产引用；包与模块加 DEPRECATED；默认 pytest 跳过 `v1_workflow_legacy`；导入守卫锁死回引。Job HTTP、`generate/*` 领域服务、`frontend_v2` Job 客户端保留。

## 相关文件

- `backend/pixelflow/agent_workflows/video/*`
- `backend/tests/test_video_agent_v21_batch_e_quarantine.py`
- `backend/tests/test_agent_video_workflow_*.py`（marker）
- `backend/pyproject.toml`（`addopts` / marker）
- `docs/superpowers/specs/2026-08-10-video-agent-v2.1-control-plane-design.md`

## 核心逻辑

1. 生产编排权威：`video_agent` Planner + Registry Tools + Adapters
2. 孤立包：`agent_workflows/video` 仅被 legacy 单测 import
3. 硬删推迟：`/agent/flows/video/*`、FE Job start/poll、Workflow DB 读取合同

## 注意事项

- 重跑隔离包测试：`uv run pytest -m v1_workflow_legacy`
- 勿在新代码 `import pixelflow.agent_workflows.video`
- 勿把批次 E 当成可以删 Job endpoint 的许可
