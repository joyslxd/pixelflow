/**
 * 旧 /flows/video Job HTTP 已删除；保留常量供 LegacyWorkspace 与契约测试 grep。
 */
/** P0-5.3：旧 /flows/video Job HTTP 已从网关移除，统一引导走 VideoAgent 对话。 */
export const LEGACY_VIDEO_JOB_CONTINUE_TIP = "请在对话中说明需求，由 VideoAgent 继续生成";
export const LEGACY_VIDEO_JOB_HTTP_REMOVED = true;
