# 用途：排除本机 Python 虚拟环境；影响：镜像仅使用 Docker 构建阶段创建的依赖，避免宿主解释器链接覆盖运行时。
backend/.venv
# 用途：排除测试缓存；影响：缩小构建上下文且不改变运行时行为。
backend/.pytest_cache
# 用途：排除 Python 字节码缓存；影响：避免将开发机缓存带入发布镜像。
**/__pycache__/
# 用途：排除 Node 依赖目录；影响：前端依赖由各自构建流程安装，不能污染 Gateway 镜像。
**/node_modules/
