---
description: VideoAgent V2.1 控制面权威设计——一切改动必须对齐本规范
alwaysApply: true
---

# VideoAgent V2.1 控制面（硬约束）

权威设计文档（唯一架构裁决依据）：

`docs/superpowers/specs/2026-08-10-video-agent-v2.1-control-plane-design.md`

任何 VideoAgent / LegacyWorkspace 兼容层 / Supervisor 视频链路的需求、设计与实现，必须严格按该文档推进；不得以临时 UX、固定路径或前端编排绕过控制面。

## 统一对话入口（产品目标）

用户始终从同一个对话入口表达意图。无论是创意讨论、贴入成熟脚本、分析参考视频、替换商品、修改第 N 镜、检查失败画面、继续生成还是导出，系统均应：

1. 读取当前 `VideoWorkspace` 和本轮输入；
2. 由 Agent 选择最小且可解释的下一批 Tool；
3. 在成本、素材选择或不可逆操作前请求明确确认；
4. 将执行事实、产物和异步任务持续写入服务端；
5. 由工作台展示、编辑和确认，而不是决定编排。

## 架构原则（实现时自检）

1. **一个控制面**：当前进度只由 `VideoWorkspace + AgentPlan/Step + Confirmation + Operation` 表达；`Workflow` 不参与 V2 当前进度计算。
2. **Tool 优先于路径**：模型只能从服务端 Registry 选已注册 Tool；入口分类只能是规划失败降级，不是正常主链路。
3. **最小前瞻计划**：每 Turn 最多 1–3 个可执行步骤；禁止一次性铺开完整脚本/成片流程。
4. **服务端拥有执行事实**：脏镜头、资产版本、确认、任务 ID、Operation 与结果引用持久化于 Workspace/Operation；浏览器缓存只能加速渲染/恢复。
5. **工作台是编辑面**：`LegacyWorkspace` 展示、编辑、提交 Turn/命令与确认；不得推导重生成范围、创建旧 Workflow、拥有 pending Job 真相。
6. **确定性边界保护模型**：schema、权限、成本、配额、幂等、revision、确认闸门由代码裁决。

## 明确禁止

- 不重写/删除 `LegacyWorkspace` 全量代码。
- 不让前端决定编排或把本地 artifact 当成唯一真相。
- 不让模型绕过额度、鉴权、确认或 Tool DTO 校验。
- 不以关键词固定路径替代 Planner 选 Tool（降级除外）。

改动前先读该设计文档相关章节与 `.spec/context-dict/video-agent/`；冲突时以设计文档为准。
