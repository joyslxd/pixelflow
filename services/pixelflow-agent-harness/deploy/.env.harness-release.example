# 用途：定义 Gateway 与 Sidecar 共享的非敏感 Harness 发布身份；影响：任一字段变化都会重算模型档案摘要并要求完整预检。
PIXELFLOW_HARNESS_PROFILE_NAME=deepseek-v4-pro
# 用途：选择 Gateway 运行 Profile；影响：决定 Content-App 地址和冻结的 Run 限额，发布时必须与目标环境一致。
PIXELFLOW_CONFIG_ENV=ec-prod
# 用途：指定直连 DeepSeek 的实际模型；影响：模型切换必须随完整发布进行，不能由单次 Run 指定。
PIXELFLOW_HARNESS_MODEL_ID=deepseek-v4-flash-vision-exp
# 用途：标识当前模型能力合同版本；影响：能力变化会改变公开模型档案摘要。
PIXELFLOW_HARNESS_CAPABILITY_VERSION=deepseek-direct-v1
# 用途：标识当前预算策略版本；影响：预算策略变化会改变公开模型档案摘要。
PIXELFLOW_HARNESS_BUDGET_VERSION=video-budget-v1
# 用途：绑定本次发布的 Gateway Tool Manifest 摘要；影响：不一致时 Gateway/Sidecar 均拒绝开放新 Run。
PIXELFLOW_HARNESS_TOOL_MANIFEST_DIGEST=sha256:请由发布脚本生成
