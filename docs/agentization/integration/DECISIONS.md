# Agent 化实施决策日志

> 只有当实现需要偏离已冻结设计/合同、或出现跨模块判断时才新增。不要把日常进度写在这里。

## D-000：设计基线

- 日期：2026-07-21
- 状态：待评审
- 决策：采用会话级 Supervisor + LangGraph workflow graphs + 现有 v2 Service/Skill 适配 + 全局 Context Runtime；使用 `frontend_v2/supervisor_v1` 双运行时渐进迁移。
- 原因：允许两人按模块合并，保留现有恢复与计费安全，并最终把会话级编排权移到后端。
- 影响：M00 合同评审通过前不进入真实代码实现。

## D-001：双长期分支自动同步

- 日期：2026-07-22
- 状态：待评审
- 决策：开发期间只允许 `feature/dev_0.8.4_boguan → feature/agent_0.8.4_boguan`；模块开始前、模块合并前强制同步检查，每天北京时间 02:00 漂移检查；共享 Agent 分支通过单槽候选和祖先门禁更新。
- 原因：避免依赖人工记忆，同时防止每次 dev push 未经测试就污染 Agent 基线。
- 影响：M00 增加 PowerShell 分支脚本、Gitee 保护/流水线门禁和自动化状态验收；M13 完成前禁止自动反向合并 Agent 到 dev。

## D-002：同模块多 Codex 的隔离单位

- 日期：2026-07-22
- 状态：已被 D-004 取代
- 决策：同一模块默认顺序执行；只有运行手册标记为 `parallel_safe` 的切片可并行，每个切片必须使用独立分支、worktree、切片状态文件和文件锁。
- 原因：多个 Codex 同时写同一分支/工作区会互相覆盖 index、状态和实现。
- 影响：模块协调 Codex负责逐个把切片分支合回模块分支；合同、migration、共享状态机和视频权威链路默认不并行写入。

## D-003：M00 合同冻结后拆分 A/B 并行线

- 日期：2026-07-22
- 状态：已被 D-004 取代
- 决策：M00 保持 5 个切片和 13.5 人时不变。M00.1–M00.2 在 `m00-contracts` 串行并产生 `contract_freeze_sha`；M00.3（B）与 M00.4（A）从该 SHA 分别在 `m00-b-web`、`m00-a-platform` 并行；M00.5 由单一集成人在 `m00-integration` 按固定顺序合并。
- 原因：Python/TypeScript 合同必须先有唯一权威基线，但合同冻结后前端镜像合同与 Git 自动化脚本没有共享写路径，可以安全并行。
- 影响：M00 不允许从第一步双写；两个并行分支不得互相 merge、不得改写对方路径或规范 fixture；所有 Codex 完整话术必须显式要求先读根目录 `AGENTS.md`。

## D-004：模块间并行、模块内切片串行与自动集成边界

- 日期：2026-07-22
- 状态：已确认
- 决策：M00 只保留 `m00-a`、`m00-b` 两条开发分支，两条线从同一个已评审合同/Agent 基线并行，各自内部严格串行；首次 M00 集成由开发者手动启动一次临时候选。M01–M12 每个模块只有一个模块分支和 worktree，模块内所有 1–3 小时切片严格串行，每个切片必须由开发者单独启动，Codex 不得自动连续执行整个模块。不同模块在依赖和文件所有权允许时可由多个 Codex 任务并行开发。
- 原因：切片子分支、切片 worktree、文件锁和二次合并显著增加认知与集成成本；逐切片手动启动能限制 Codex 单次任务长度，并由仓库状态、commit 和测试证据保证跨对话恢复。
- 影响：删除 `parallel_safe`、`Start-AgentSlice.ps1`、切片子分支和模块级托管话术。单个切片内的分支恢复、TDD、测试、审核、状态记录、commit、push 仍由 Codex 自动完成。普通模块最后一个切片把状态写为 `ready_for_integration` 后，由 Gitee/Jenkins 单槽流水线自动纳入最新 Agent/dev 并集成；失败写 `integration_blocked`。每天 02:00 的漂移检查依赖 M00 和一次性远端管理员配置，不能由已经结束的 Codex 对话自行唤醒。
