# M04.5 测试报告

## 结论

M04.5 在 M04.1–M04.4 的结构化摘要、四阈值 Coordinator 和可恢复队列之上，增加了保存前 `SummaryVerifier` 与压缩生命周期 Outbox。关键事实基线逐项精确验证用户目标、已确认决定、否定约束、Workflow 状态、未决问题、Artifact 证据和稳定 ID；候选摘要缺失事实、篡改内容 hash 或跨会话时均 fail-closed。

压缩取得租约后先写 started，每个成功动作写 progressed。Memory/SQL Repository 在同一内存临界区或数据库事务中完成 fencing 校验、completed/failed 终态事件写入和队列状态迁移；租约过期或已被接管的陈旧 worker 不能写伪 completed/failed。失败与暂停保留 `retry_required`，不会放行超窗上下文或要求前端重发输入。

本切片没有删除或改写原始消息，没有把 Plan、创作合同、场景蓝图、资产清单、pending action、pending job 或 operation 放入摘要。没有新增配置、修改 `status/BOARD.md`、调用真实付费 API 或修改两个长期 feature 分支。

## 安全预检

- 模块分支：`codex/agent-0.8.4-m04-context-compaction`
- 切片基线：`45c4a5c12e5e873bb97e0ffea0707d68174f8b23`
- 冻结 Agent：`d20762935ad8bd994a24e332f4237da7a1aaf591`
- 模块 worktree：`E:\IntelliJIDEA\secondWorkSpaces\cmyqCode\pixelflow-worktrees\m04-context-compaction`
- dev→agent：远端 dev 是远端 Agent 的祖先；M01、M03 状态均已进入 Agent。
- 远端模块分支、本地 HEAD 和上一切片证据一致；模块分支仅有一个 worktree，预检没有用户未提交改动。
- 唯一写入者由 `Start-AgentModule.ps1` 领取为 `Codex-A-M04.5`；长期 feature 分支保持只读。

## TDD 过程

1. `SummaryVerifier` 首轮因模块不存在而收集失败；最小实现后专项为 `11 passed`。
2. `SummaryBuilder` 接入验证基线时先因缺少新参数得到 `1 failed`；补齐基线和缺失否定约束的 fail-closed 用例后，摘要验证与 Builder 为 `30 passed`。
3. 稳定 ID 自审补充 `plan-1` 不得匹配 `plan-10` 的前缀碰撞用例，先得到 `1 failed`，改为稳定 ID 边界匹配后为 `31 passed, 1 warning`。
4. Outbox 首轮因 `RepositoryCompactionEventOutbox` 不存在而收集失败；最小 Adapter 和 Runtime 生命周期实现后为 `7 passed`。随后加入事件端口必填、进度写入失败、暂停、异常和 `already_running` 用例，M04.5 定向集合为 `93 passed, 1 warning`。
5. 独立审核复现陈旧 worker 在 fencing 收尾前写 completed，形成 `started → completed → failed`。新增回归用例先稳定得到 `1 failed`，随后把 Memory/SQL 的 fencing 校验、终态 Outbox 与队列迁移收口到同一临界区/事务，并强制 EventSink 与 Queue 绑定同一 Repository；整改后事件与队列专项为 `39 passed, 1 warning`。
6. Final 门禁 Pester 先得到 `35 passed, 1 failed`，证明 M04 权威清单尚未配置；补齐清单后为 `36 passed`。首次 M04 Final 因 Alembic `fileConfig` 污染同进程 logger 导致一个既有日志断言失败；独立复现确认测试单独运行通过，门禁将 Runtime 与 DeerFlow 边界拆为两个 pytest 进程后保持原断言并转绿。

## 最终验证

- M04 Final 权威门禁：最终树执行 `Invoke-AgentModuleGate.ps1 -ModuleId M04 -GateType Final -ChinesePolicyBaseRef 45c4a5c12e5e873bb97e0ffea0707d68174f8b23`，结果 `Passed=True`、`CommandCount=5`。
- 全部 18 个 `test_agent_runtime_*`：独立 reviewer 鲜跑 `333 passed, 1 warning`。
- DeerFlow summarization、dynamic context 与 Harness 边界：独立 reviewer 鲜跑 `39 passed, 1 warning`。
- BranchAutomation Pester：`36 passed, 0 failed`。
- 变更 Python 路径 `ruff check` 与 `ruff format --check`：通过。
- `git diff --check`：通过；中文工程规范和带 BaseRef 的分支策略在中文提交后重新运行。
- 中文门禁首次把 Python 列表解包的行首 `*` 误识别为人工注释；改为行为等价的显式 `extend` 后，摘要验证与 Builder 仍为 `31 passed, 1 warning`，再由同一中文门禁验证。

唯一 warning 来自既有 LangGraph pending deprecation，不是本切片新增。

## 独立审核

首轮只读审核发现 Important 2、Critical 0：

- 陈旧 worker 可能在 fencing 拒绝前写 completed，形成矛盾终态。
- 设计说明已先写最终门禁完成，但状态和测试报告尚未收口，完成材料不一致。

第一项已按 TDD 改为原子终态收尾并增加 Memory/SQL、陈旧 worker 和异 Repository 绑定拒绝用例；第二项通过本报告和最终状态统一收口。最终复审结论为 Critical 0、Important 0、Minor 0，“是否可提交：是”。reviewer 独立鲜跑 M04.5 定向 `98 passed`、全部 Runtime `333 passed`、DeerFlow/Harness 边界 `39 passed`、Pester `36 passed, 0 failed`，Ruff、9 个文件的 format check 和 `git diff --check` 均通过；额外 SQL 陈旧租约验证只保留 started 且接管租约不变。

## 风险与边界

- `SummaryVerificationBaseline` 必须由调用方根据当前业务权威状态构建；Verifier 不读取或改写业务合同，只负责证明候选摘要包含调用方明确列出的事实。
- Outbox Adapter 在并发 sequence 冲突时重新读取尾部并有限重试；终态事件由 Queue/Event 组合 Repository 在 fencing 事务内生成，耗尽或持久化异常均 fail-closed。
- 原始消息始终保留；摘要只覆盖连续消息范围。92% 硬闸门失败只能进入 minimal-safe 或 paused，不会继续模型调用。
- M04.5 是模块最后一片，不是阶段中间检查点；完整 M04 Final 绿色后才能写 `ready_for_integration`，随后由开发者手动复制 9.10A 话术启动单槽最终集成。
