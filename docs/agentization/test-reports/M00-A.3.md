# M00-A.3 测试与审核记录

- 模块/切片：`M00-A / M00-A.3`
- 分支：`codex/agent-0.8.4-m00-a`
- 前置提交：`d15c064093b0dbc64097fef3b5967e329546c9d0`
- 共同 Agent 基线：`8e626ae232d984f14fa9954b672b4e025894d426`
- 测试类型：本地非付费、临时 Git 仓库、后端回归、独立只读审核

## TDD 证据

1. 初始 RED：在 Windows PowerShell 5.1 中仅对当前进程使用 `Set-ExecutionPolicy Bypass` 后运行 Pester；15 个测试中 `4 passed, 11 failed`，失败点为七个自动化入口尚不存在和 `.worktrees/` 尚未忽略。
2. 边界 RED：增加冻结测试矩阵中的串行与状态用例后，稳定复现上一切片 commit 未 push 仍可续片、成功/失败状态未回写、第二阶段检查点状态 commit 分叉、blocked 错写 `last_integrated_commit` 等问题。
3. GREEN：按最小安全状态机修复后，原 M00-A.3 Pester 为 `36 passed, 0 failed`。
4. 历史兼容 RED：新增真实历史测试后，定向 Pester 为 `10 passed, 1 failed`，唯一失败同时列出 `0af72ff` 英文标题和原始英文 docstring。
5. ignore-revs RED：把后续修改提交写入 `blame.ignoreRevsFile` 后，Git 会把修改行重新归因到 `0af72ff`，门禁错误放行，定向 Pester再次稳定为 `10 passed, 1 failed`。
6. 历史兼容 GREEN：门禁显式使用空 `--ignore-revs-file=` 后，真实历史、后续修改、ignore-revs 和复制原文边界均通过，定向 Pester `12 passed, 0 failed`，完整 Pester `39 passed, 0 failed`。

覆盖范围包括：

- 模块分支/worktree 创建、幂等恢复、丢失恢复、安全快进、未 push 和跳片拒绝；
- dev→agent 无漂移、绿色同步、门禁失败、合并冲突、脏工作区、缺少远端和远端 SHA 最终 guard；
- 阶段检查点白名单与 metadata/commit 绑定、R1→R2 增量、重复旧检查点拒绝；
- 单槽锁、成功原子更新 Agent+模块状态、失败 blocked 证据、修复后重试、幂等确认；
- M00-A/B 文件所有权与普通集成入口拒绝，避免绕过 M00-I.1；
- 中文提交标题/正文、人工注释、多行 Python docstring、YAML 数组叶子和 JSON schema 逐项说明；
- 门禁启用前完整 SHA 精确豁免、行级 blame、ignore-revs 防绕过、后续修改和复制原文防回退。

## 通过记录

| 命令/检查 | 结果 |
| --- | --- |
| `Invoke-Pester -Script scripts\agentization\tests -PassThru` | `39 passed, 0 failed` |
| 共同基线中文门禁 `8e626ae..HEAD` | `Passed=True`；精确豁免 `0af72ff` 及其原始行 |
| M00-A Final gate（Pester + 合同/不变量/OpenAPI + Ruff） | 实现提交 `db3dbdc` 后、状态仍为 `blocked` 时返回 `Passed=True`，共 4 个命令 |
| `pytest`：合同、不变量、OpenAPI、harness boundary、conversation router、task store | `65 passed, 1 warning`；warning 为既有 LangChain pending deprecation |
| `ruff check pixelflow/agent_runtime ...` | `All checks passed` |
| PowerShell AST 解析 | 全部脚本无语法错误 |
| `git diff --check` | 通过 |

## 独立审核

- reviewer：`/root/m00_a3_readonly_review`，只读，不修改文件、不操作分支。
- 审核方式：逐轮检查分支恢复、checkpoint、状态 commit、原子 push、中文门禁与测试矩阵；每轮 P1 都先增加 RED 用例再修复。
- 最终结论：未发现本地 P0/P1；Pester `36 passed, 0 failed`、`git diff --check` 通过。终审指出的成功后旧失败证据残留已继续清理并补充断言。
- 历史兼容 reviewer：`/root/m00_a_grandfather_review`，只读，不修改文件、不操作分支。
- 首轮结论：无 P0；发现 ignore-revs 可绕过 blame、状态恢复顺序过早两个 P1，并建议强化复制原文与精确错误断言。
- 复审结论：两个 P1 和测试建议均已关闭，“可继续”，无 P0/P1；仅有两处文档计数 P2，已在提交前修正。

## 未执行与边界

- 未运行 M00-B/Web 全量聚合；A 线未修改 `web/**`，跨端 fixture、Web 构建和 flag-off 聚合由 M00-B/M00-I.1 负责。
- 未配置或验证 Gitee/Jenkins 全局单槽、保护分支、WebHook、北京时间 02:00 调度和失败通知；本切片只能把自动化状态推进到 `automation_local_ready`。
- 未调用真实图片、视频、PPT、剪映、LLM 或其他付费 API。

## 模块级历史兼容结论

- `0af72ff6993e9e67636f21e8e16d641411702d67` 产生时尚未启用中文门禁，现按用户明确批准使用完整 SHA 和逐行 blame 精确豁免，不 rebase、不 force-push。
- M00-A 无剩余硬阻塞，状态恢复为 `ready_for_integration`；自动化仍为 `automation_local_ready`。
- 远端 M00-B 最新提交 `efadb5d` 已把共同基线 Plan UI 测试修复为 `55/55`，聚合入口为 `205/205`，B 状态也为 `ready_for_integration`。本任务未修改 B 线；A/B 就绪后仍停在等待开发者手动启动 `M00-I.1`。
