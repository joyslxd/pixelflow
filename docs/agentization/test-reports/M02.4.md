# M02.4 composition、独立 graph ID 与 lifespan 测试及审核记录

## 范围与基线

- 模块/切片：`M02 / M02.4`
- 分支：`codex/agent-0.8.4-m02-graph-kernel`
- Agent 基线：`390e2a3203dada5df1507a4a722c4efe03ce7365`
- 上一切片远端提交：`2dd812a68e15d7f83cd9f599dc95b9c60bbd99ba`
- 远端 dev：`fb7450775a227d891372c19eae1b308045c51e68`
- 写入范围：新图 composition、Gateway lifespan 装配、独立 graph ID、配置键中文映射、M02.4 测试、M02 Final 门禁、状态和本报告。
- 禁止范围：未修改旧 `backend/pixelflow/graph.py`、旧 `/agent/flows` 路由、两个长期 feature 分支、依赖或其他模块；没有创建切片子分支/worktree。
- 外部调用：全部使用本地 LangGraph、Memory/SQLite checkpointer、fake Workflow 和本地门禁；未调用图片、视频、PPT、剪映、LLM 或其他真实付费 API。

## 安全预检

1. 完整读取根 `AGENTS.md`、最新主流程设计、全部 Agentization 开发入口文档、总看板、M02 状态和 M02.1–M02.3 测试报告。
2. fetch 后确认远端 Agent 为 `390e2a32`、远端 dev 为 `fb745077`，dev 是 Agent 祖先；`Sync-DevToAgent.ps1` 返回 `up_to_date`，没有生成同步候选或修改长期分支。
3. 确认 M00 集成提交 `9b7a292`、M01 集成提交 `337a191` 均为远端 Agent 祖先，M02 依赖满足。
4. 模块本地、远端 HEAD 均为 `2dd812a`，上一切片中文提交、测试、审核和写入权释放证据完整；模块只有一个 worktree，开工前干净。
5. `Test-AgentBranchPolicy.ps1` 通过后，`Start-AgentModule.ps1` 只恢复既有 `m02-graph-kernel` worktree 并领取 M02.4；没有创建额外分支或 worktree。
6. 开工基线为 `124 passed, 1 warning`；唯一 warning 是既有 LangGraph pending deprecation。

## 实现合同

- `compose_agent_runtime_graph()` 复用冻结的 SupervisorState、Workflow registry/dispatcher、interrupt/resume 与 projection 原语，不复制业务状态机。
- 新 graph ID 固定为 `pixelflow_agent_runtime`；旧 `pixelflow` 和 `lead_agent` 注册值不得删除、改名或替换。
- Gateway 新图必须复用 lifespan 已创建的共享 checkpointer；退出时只关闭 composition 引用，由外层继续拥有并最终关闭 Store/checkpointer。
- Memory 图对象重建和文件 SQLite saver 关闭重开后，都必须从原 interrupt ID 恢复；跨 conversation 即使 workflow ID 相同也不得串状态。
- 旧 `/agent/flows` 创建、SSE 和资产路由必须由 Final 权威门禁直接回归。
- `langgraph.json` 新增叶子键必须在同一变更中提供一一对应的中文用途、影响和兼容边界说明；提交级自动门禁要求同目录 schema 覆盖全部配置叶子。

## TDD 证据

1. 基线 GREEN：M02.1–M02.3 与相邻 runtime 回归为 `124 passed, 1 warning`。
2. 初始 RED：先新增 composition、Memory/SQLite 重启、跨 conversation、Gateway 生命周期和 graph 注册测试，收集阶段因 `app.gateway.pixelflow_agent_runtime` 不存在失败。
3. 首轮 GREEN：增加最小 composition、Gateway context manager 和 graph 注册后，聚焦测试达到 `5 passed`；两项既有 Gateway recovery 测试因使用不合法的普通对象假 checkpointer 失败。
4. 环境边界修复：把既有测试 fake 改为真实 `InMemorySaver`，同时断言共享所有权和退出清理，聚焦达到 `7 passed, 1 warning`。
5. 门禁 RED/GREEN：BranchAutomation 先要求 M02 Final 权威清单，旧门禁 fail-closed 得到 `41 passed / 1 failed`；配置清单后为 `42 passed`。
6. 独立审核 RED/GREEN：reviewer 指出旧 `/agent/flows` 路由不变量未被直接执行；先把 `test_pixelflow_task_store.py` 加入计划期望并确认计划校验 RED，再补入 M02 Final pytest/Ruff 清单，计划断言及旧路由测试转绿。
7. 中文配置 RED/GREEN：首个本地提交执行中文工程门禁时，因 `langgraph.json` 缺同目录 `langgraph.schema.json` 被拒绝；补齐全部配置叶子的中文“用途/影响” description 后重新校验。

## 最终验证

| 类别 | 命令/范围 | 结果 |
| --- | --- | --- |
| M02 权威 Python 回归 | 图 state/dispatcher/interrupt/composition、checkpointer、run manager、Gateway、harness、旧 graph、旧路由、OpenAPI、LangGraph auth | `171 passed, 1 warning` |
| 旧路由聚焦 | `test_pixelflow_task_store.py::test_pixelflow_router_imports` | `1 passed, 1 warning` |
| 分支自动化 | `BranchAutomation.Tests.ps1` | `42 passed, 0 failed` |
| Python 静态检查 | M02 Final 权威 Ruff 清单 | `All checks passed` |
| 工作树格式 | `git diff --check` | 通过；仅有 Windows `core.autocrlf` 换行提示 |
| 完整模块门禁 | `Invoke-AgentModuleGate.ps1 -ModuleId M02 -GateType Final -ChinesePolicyBaseRef 390e2a32...` | `Passed=True`、`CommandCount=5` |
| 中文工程规范 | 中文提交、注释/docstring、`langgraph.schema.json` 配置逐项说明 | 通过 |

唯一 warning 为既有 LangGraph `allowed_objects` 默认值将变化的 pending deprecation，本片未修改相关依赖。

## 独立审核

- Reviewer：`/root/m02_4_fresh_independent_review`，是本轮新建的独立只读 reviewer，全程未修改、暂存、提交或推送文件。
- 首轮结论：Critical 0、Important 1、Minor 0，`Ready to commit: No`；唯一问题是旧 `/agent/flows` 路由不变量未被 Final 权威清单直接锁定。
- 整改：将现有 `test_pixelflow_task_store.py` 纳入 M02 Final pytest/Ruff 和 BranchAutomation 计划断言，直接覆盖创建、SSE 和资产路径。
- 复审结论：Critical 0、Important 0、Minor 0，`Ready to commit: Yes`；reviewer 只读验证 `171 passed`、Pester `42 passed`、Ruff 和差异检查均通过。

## 结论与下一步

M02.4 是模块最后一片，不是 `phased-rollout-plan.md` 明确列出的中间检查点。完整 M02 Final 门禁绿色后，模块进入 `ready_for_integration` 并释放唯一写入权；本任务只提交、推送模块分支后停止，不更新总看板，不自动进入 M05 或执行最终集成。

当前自动化状态为 `automation_local_ready`。开发者必须新开任务，复制执行手册 9.10A 话术并明确模块号 `M02`，手动启动唯一单槽最终集成。
