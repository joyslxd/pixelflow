# M13.1 / R1 统一上下文预算与压缩恢复修复记录

- 模块：`M13`
- release：`R1`
- slice：`M13.1` 集成后的修复增量
- 分支：`feature/agent_0.8.4_boguan`
- 日期：`2026-07-26`
- 自动化状态：`automation_local_ready`
- 生产状态：未发布；`config.prod.yml` 继续保持 `off / [] / 0 / false`

## 修复目标

真实视频长对话曾把完整 Plan 修订恢复请求重复计入模型输入，使当时约
`307,110 tokens` 的业务上下文越过旧档案可用上限，并在压缩失败后被
Snapshot/SSE/Run 读取反复立即唤醒。本次修复同时冻结以下跨 R1–R4 合同：

- DeepSeek V4 Pro 物理上下文窗口为 `1,000,000 tokens`；
- 全部当前和未来 Agent/节点统一使用 `896K` 有效窗口、`32K` 输出预留、
  `32K` 安全预留，`K=1024 tokens`；
- 可用输入固定由公式计算为 `851,968 tokens`，而不是手写第二份常量；
- 实际 Runtime 必须使用当前有效且已验证的模型档案，不能走 128K 业务兜底；
- 压缩失败持久化 30 秒重试边界，边界前读取接口只读不调度；
- 恢复专用 Plan 修订数据保留在 Conversation Store，但不重复进入 Prompt。

## 实现结果

1. `ContextBudgetConfig` 成为配置 DTO，dev/prod 均逐项声明
   `effective_context_k=896`、`output_reserve_k=32`、
   `safety_reserve_k=32`、`require_verified_model_profile=true` 和
   `compaction_retry_backoff_seconds=30`。
2. `ContextBudgetPolicyProvider` 对任意非空节点名返回同一份启动快照，
   删除 Supervisor、图片、视频、PPT、视频分析和摘要节点之间的窗口分叉。
3. `deepseek-v4-pro` 的 `models[].context_profile` 在 dev/prod 都声明
   `max_context_tokens=1000000`、`max_output_tokens=32768` 和验证元数据。
4. Gateway 只把 `context_profile` 提供给 PixelFlow Runtime；
   模型 Factory 明确不把该内部字段透传给 LangChain/DeepSeek Provider。
5. Memory/SQL 压缩锁都保存 `retry_not_before`，成功路径拒绝携带重试时间，
   失败路径拒绝缺失或非未来时间。
6. Snapshot、SSE 和 Run 查询只在重试边界到达后单飞恢复，避免轮询产生重试风暴。
7. `pendingPlanRevisionRequest` 及 snake_case 别名与原有恢复专用字段一起从
   模型投影排除，权威 Store 内容保持不变。
8. `AGENTS.md`、最新设计和 `docs/agentization` 原始架构、合同、阶段计划、
   工作拆分、测试矩阵、决策、执行手册均已同步；R2、R3、R4 启动话术显式继承
   本合同。

## 自动化证据

| 门禁 | 结果 |
| --- | --- |
| Runtime 重点回归 | 最终复跑 `232 passed`，覆盖预算、档案、压缩、队列、退避、四类 intent、附件和生命周期 E2E |
| 四类 intent 代表性用例 | `image / video / ppt / video_analysis` 各自通过 520K 历史上下文自动压缩，并保留当前图片 `materials` |
| 后端完整可运行集合 | `4583 passed, 19 skipped` |
| 后端全量已知基线 | 另有 6 个 `test_docker_sandbox_mode_detection.py` 用例因仓库不存在 `scripts/docker.sh` 失败，与本次变更和 Runtime 无关 |
| 前端测试 | `303 passed`，含压缩 Notice、排队 badge、Snapshot/SSE 恢复和上传 Client 合同 |
| 前端类型检查 | `corepack pnpm lint` 通过 |
| 前端测试构建 | `corepack pnpm build-test` 通过；只有既有 chunk 大小提示 |
| Python 静态检查 | 相关 Runtime、Gateway、模型 Factory 和测试文件 `ruff check` 通过 |
| Mac Chrome 可见验收 | 真实图片上传、视频表单、3 个创意方向、自动压缩、排队可见、过期租约接管和排队输入继续执行均通过 |

严格档案启用后，后端全量首次额外暴露 5 个使用 `fake-test-model` 却没有
`context_profile` 的 E2E 测试夹具。修复方式是为测试替身补充已验证档案，
没有放松生产 fail-closed；对应 6 个生命周期/HTTP E2E 随后全部通过。

## 本地真实 Runtime 证据

在本机 dev profile 和真实 SQLite Store 中新建独立测试对话，预置
`560,117 bytes` 的历史 Artifact，再提交一条 30 秒、9:16 视频需求，
当前输入携带 1 个参考图 material。真实路径产生以下持久化事件：

```text
context.compression_started
context.compression_progressed(action=externalize_payloads)
context.compression_completed
```

完整 Artifact 已写入 `pixelflow_agent_context_payloads`，原消息未删除；
当前视频输入长度和 1 个 material 均完整保留，Turn 在压缩完成后进入
`processing`。该验证没有调用图片、视频、PPT、剪映等付费生成接口。

## Chrome 可见流程状态

用户在 MacBook 的 Chrome 扩展中开启“允许访问文件网址”后，已在可见浏览器中
创建独立对话 `779260a12e354799bbd8be8d613cb8ee`，并完成以下真实流程：

1. 通过 Chrome 文件选择器上传本机 PNG；页面显示“已添加 1 个素材”，随后获得
   自有 TOS HTTPS 地址并随视频需求保存。
2. 提交 30 秒、9:16、电商种草视频需求，补全产品、品类、目标人群、转化目标、
   视频/图片模型、清晰度、用途和风格表单，真实生成 3 个创意方向。
3. 在该对话预置 `600,114 bytes` 历史 Artifact 后提交补充输入，持久化事件依次为
   `context.compression_started`、`context.compression_progressed`
   和 `context.compression_completed`；Artifact 已外置且当前图片输入未丢失。
4. 使用短期活跃租约模拟“压缩仍在执行”的并发窗口，再提交一条输入。Chrome
   明确显示“已排队 1 条 · 第 1 位”；租约到期并重新加载权威 Snapshot 后，
   后端再次完成压缩并产生 `queued -> processing -> completed`，页面继续执行该输入。
5. 选择推荐方向“从失控到掌控：30秒权限大扫除”后，首次策划 job 持续返回
   `running`，最终于 `2026-07-26 19:14 CST` 被前端报告为
   `Plan generation polling timed out`；页面恢复为“创作规划 · 需处理”，
   三个方向和当前选择均未丢失。从同一选择入口发起一次受控重试后，于
   `19:20 CST` 成功生成完整 plan.md v1。页面当前停在“创作规划 · 待确认”，
   未点击“同意方案”，也未调用任何付费素材或视频生成入口。

## Mac 本地工程检查

当前设备是 macOS，因此使用 `git`、`rg` 和 `python3` 执行本机等价检查，而没有把
Windows PowerShell 当作必需运行时：

- `705c76a..HEAD` 的 10 个提交标题和非空正文都包含中文主体语义；
- 本次新增人工注释/docstring 未发现只有英文语义的说明；
- 本次新增的 dev/prod 上下文预算和模型档案叶子项均有紧邻中文“用途/影响”说明；
- `git diff --check` 仍作为提交前独立检查执行。

仓库权威 M00 脚本仍是 Windows PowerShell 5.1/Pester 合同；Mac 不伪装其执行结果。
按该脚本“只要配置文件发生变更就扫描整个文件”的完整口径，现有
`config.dev.yml/config.prod.yml` 仍包含本次变更之前的未逐项说明叶子配置，
因此不把 Windows 中文规范总门禁标记为绿色，也不据此执行 push 或阶段集成。

## 安全边界

- 未把用户 Authorization 写入代码、配置、文档、日志或测试报告。
- 未调用真实付费图片、视频、PPT、剪映生成接口。
- 未修改生产 Runtime 模式、intent、rollout 或 Feature Flag。
- 未启动 M13.2 / R2，也未自动执行 Agent→dev 合并、push 或生产发布。
- 自动化状态保持 `automation_local_ready`。
