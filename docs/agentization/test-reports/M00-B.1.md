# M00-B.1 测试报告

## 范围与基线

- 切片：`M00-B.1`
- 分支：`codex/agent-0.8.4-m00-b`
- Agent 基线：`8e626ae232d984f14fa9954b672b4e025894d426`，与 M00-A 状态记录一致。
- 写入范围：TypeScript 镜像合同、前端合同测试、跨平台 Web 测试入口、测试命令中文说明和本线状态/报告。
- 禁止范围：未修改 Python 权威 DTO/fixture、M00-A 路径、`feature/dev_0.8.4_boguan` 或 `feature/agent_0.8.4_boguan`。
- 外部调用：未调用图片、视频、PPT、剪映、LLM 或其他真实付费 API。

## dev→agent 预检

1. `git fetch --prune origin` 成功；根工作树干净，远端不存在 M00-B 分支，也没有同模块 worktree 或写入者。
2. `origin/feature/dev_0.8.4_boguan` 不是当前 Agent 的祖先，说明最新 dev 尚未同步。
3. M00-A 已推送并明确记录共同基线 `8e626ae232d984f14fa9954b672b4e025894d426`。为满足 A/B 同源且不直接写两个长期分支，本线从该 SHA 创建独立 worktree；最新 dev 留待 `M00-I.1` 按固定顺序纳入临时候选。

## TDD 证据

1. RED：先新增 `web/tests/agentRuntimeContracts.test.mjs`，将环境变量指向预期但尚不存在的编译模块后执行 `node --test tests/agentRuntimeContracts.test.mjs`；Node 以 `ERR_MODULE_NOT_FOUND` 和退出码 `1` 失败。
2. GREEN：新增 TypeScript 镜像合同、类型 shape 测试和跨平台编译/执行入口后，`pnpm test:agent-runtime-contracts` 得到 `6 passed`。
3. Reviewer 首轮指出 Context、External Job 和事件边界与 M00-A 权威 DTO/fixture 不一致；实现线只读核对权威文件后修正 TypeScript 与测试，未修改任何 Python 文件。复审结论为 `Ready`。

共同基线整改继续按 TDD 执行：

1. RED：执行 `node --test tests/mainFlowContract.test.mjs`，共 `55` 项，`52` 项通过，3 个 Plan 流程断言失败。
2. 根因：旧断言要求同步 `api.revisePlanMarkdown`，并要求点击 handler 内直接保存 Plan；当前实现已改为“启动并持久化 Plan job，再由共享恢复路径执行严格 `plan_save` 消息任务”。
3. GREEN：测试改为分别验证 handler 的 `start → persist → resume` 顺序，以及 `step.result → savedMessage → 服务端 Plan artifact → conversation context` 权威链路；`mainFlowContract` 恢复为 `55/55`。
4. 整改只修改前端合同测试，没有修改 `WorkspacePage.tsx`、Python DTO/fixture 或供应商调用代码。

合同测试覆盖：

- 编排模式、九类 Action、五类 intent、Workflow/Turn/External Job 状态和首批事件枚举；
- `ActionDecision`、`WorkflowRecord`、`TurnRecord`、`ExternalJobRef`、`ContextSummary`、`ContextBudgetReport`、`ContextEnvelope`、`ContextRequest`、`OperationRequest` 和 `TurnStartRequest` 的 TypeScript shape；
- External Job 在 `created` 阶段允许 provider job 和下次轮询时间为 `null`；
- wire event 仅接受 schema v1、`sequence >= 1`、冻结事件类型、ISO-8601 时间、JSON 对象 payload 和精确顶层字段集合；
- 非法版本、未知事件、序号 `0`/负数/小数、额外顶层字段、空标识、非法时间和非对象 payload 均被拒绝。

## 最终验证

| 类别 | 命令/范围 | 结果 |
| --- | --- | --- |
| Plan 流程定向 | `node --test tests/mainFlowContract.test.mjs` | `55/55` 通过 |
| 合同定向 | `node scripts/run-tests.mjs --contracts` | `6/6` 通过 |
| 跨平台聚合入口 | `node scripts/run-tests.mjs` | `205/205` 通过 |
| TypeScript 静态检查 | `node node_modules/typescript/bin/tsc --noEmit` | 通过 |
| 生产构建 | `node node_modules/typescript/bin/tsc -b && node node_modules/vite/bin/vite.js build --mode production` | 通过；仅有既存 chunk 体积提醒 |
| 工作树格式 | `git diff --check` | 通过 |
| 中文规范 | 人工逐项检查新增/修改注释、状态、测试、配置说明和提交文案 | 通过；新增代码没有英文解释性注释，两个 package script 均在 `web/TESTING.md` 一一提供中文用途与影响说明 |

`corepack pnpm install --frozen-lockfile` 因本机 Corepack 签名 keyid 状态失败；改用已安装的同类包管理器命令 `pnpm install --frozen-lockfile`，锁文件未变化且依赖安装成功。该环境问题不影响测试入口代码。

本次整改关闭的 3 个共同基线失败为：

1. `plan revision defaults to modifying the current creative and only regenerates directions on explicit choice`
2. `current-creative Plan revision persists the v3 message before context`
3. `initial v1 Plan uses the recoverable strict message job before writing context`

整改没有通过 skip、白名单或强制成功隐藏红灯，也没有恢复旧同步 API。测试现在验证可恢复 Plan job 和严格消息保存的真实编排边界，聚合入口由 `202/205` 恢复为 `205/205`，M00-B 本线可进入 `ready_for_integration`。

## 独立审核与安全

- Reviewer：`/root/m00_b1_reviewer`，全程只读。
- 首轮结论：`With fixes`；一个 Critical、两个 Important、一个 Minor。
- 处理：补齐 Context 摘要/预算/请求合同，修正 External Job nullable 字段，收紧事件 sequence 和额外字段，补完整类型/运行时测试。
- 复审结论：`Ready`；上一轮意见全部关闭，无新增阻断或未解决问题。
- Reviewer 独立复跑：合同测试 `6/6`、TypeScript lint、`git diff --check` 均通过；确认没有越权修改 Python、M00-A 或长期分支。

共同基线整改审核：

- Reviewer：`/root/m00_b_baseline_reviewer`，全程只读。
- 首轮结论：`With fixes`；两项 Important，分别是 revision choice 清理阶段的错误测试语义，以及只检查调用存在但未绑定执行顺序和服务端消息来源。
- 处理：区分 pending job 启动期清理与最终权威 context 写入；增加 `start → persist → resume` 顺序断言，并绑定 `step.result → savedMessage → planContextFromSavedMessage(savedMessage, ...) → updateConversationWithProgress`。
- 复审结论：`Ready`；无未解决 Critical/Important。Minor 为 `regenerate_directions` 仍是源码正则合同测试，但已限定具体 handler 且只有一次 `startDirectionJob`，不阻断本次整改。
