# M00-A.1 测试报告

## 范围与基线

- 切片：`M00-A.1`
- 分支：`codex/agent-0.8.4-m00-a`
- Agent 基线：`8e626ae232d984f14fa9954b672b4e025894d426`
- 测试对象：本报告所在提交；避免在提交内容中记录自引用、会随 amend 改变的 commit SHA。
- 变更范围：仅新增旧 v2 流程 characterization tests 与 M00-A 状态/测试记录；未修改生产代码、`web/**`、M00-B 路径或两个长期 feature 分支。
- 环境：Windows / PowerShell，Python `3.12.13`，pytest `9.0.3`，ruff `0.15.12`，Node `v24.17.0`，pnpm `11.11.0`。

## TDD 证据

1. RED：创建测试文件前，在 `backend/` 执行 `python -m pytest tests/test_agent_runtime_legacy_invariants.py -q`，pytest 以“file or directory not found”退出，证明目标测试尚不存在。
2. GREEN：仅新增 `backend/tests/test_agent_runtime_legacy_invariants.py`，未修改生产实现；同一命令得到 `4 passed`。
3. 测试锁定以下旧行为：
   - Plan 和视频场景包必须人工确认；图片结果保持 60 秒审核窗口。
   - intake/Plan/image/scene/video/PPT pending job 元数据可原样恢复，普通状态更新不丢失 job ID。
   - 既有前端恢复合同证明 image/video/scene-package 恢复只查询原 job，不调用 `/start` 创建重复任务。
   - `quota_paused` 与 `quota_insufficient` 可从原 job 查询结果中恢复。
   - 只有显式保存最终产物下载元数据后才满足交付完成条件。

## 最终验证

| 类别 | 命令/范围 | 结果 |
| --- | --- | --- |
| 新增 characterization | `python -m pytest tests/test_agent_runtime_legacy_invariants.py -q` | `4 passed`；1 条现有 LangChain pending deprecation warning |
| 后端定向 + OpenAPI | 新增文件 + planning/image/video/PPT/task store 8 个既有定向用例 + `tests/test_openapi_operation_ids.py` | `15 passed`；1 条同上 warning |
| Python 静态检查 | `python -m ruff check tests/test_agent_runtime_legacy_invariants.py` | 通过 |
| 人工/定时确认边界 | `node --test tests/reviewWindow.test.mjs` | `3 passed` |
| 真实确认合同（筛选门禁） | `node --test --test-name-pattern="image edit options load.*|video results require explicit.*|active assistant progress.*" tests/mainFlowContract.test.mjs` | `3 passed` |
| 恢复不重复启动（筛选门禁） | `node --test --test-name-pattern="restored conversations resume existing.*without starting duplicates" tests/mainFlowContract.test.mjs` | `3 passed` |
| 下载交付与暂停边界 | 编译 `src/lib/workflowTaskBoard.ts` 后执行 `node --test tests/workflowTaskBoard.test.mjs` | `11 passed` |
| 工作树格式 | `git diff --check` + 暂存四个切片文件后的 `git diff --cached --check` | 通过，包含此前 untracked 的新增文件；Git 仅提示既有行尾策略会在工作树使用 CRLF |

后端定向命令覆盖：

```text
tests/test_agent_runtime_legacy_invariants.py
tests/test_pixelflow_planning_router.py::test_planning_router_creates_reviewable_plan_markdown
tests/test_pixelflow_image_router.py::test_image_router_starts_generate_job_and_polls_result
tests/test_pixelflow_image_router.py::test_image_router_generate_job_marks_quota_paused
tests/test_pixelflow_video_router.py::test_video_router_prepares_scene_packages
tests/test_pixelflow_video_router.py::test_video_router_starts_prepare_scene_package_job_and_polls_result
tests/test_pixelflow_video_router.py::test_video_router_scene_asset_job_quota_paused
tests/test_pixelflow_ppt_router.py::test_ppt_router_pauses_job_on_quota_insufficient
tests/test_pixelflow_task_store.py::test_memory_conversation_store_updates_message_by_client_message_id
tests/test_openapi_operation_ids.py
```

## 已授权的既存 M00-B/Web 基线问题

`node --test tests/mainFlowContract.test.mjs` 在未改动的原 Agent worktree 与本 M00-A worktree、同一基线 `8e626ae232d984f14fa9954b672b4e025894d426` 上结果相同：`52 passed / 3 failed`。失败用例为：

1. `plan revision defaults to modifying the current creative and only regenerates directions on explicit choice`
2. `current-creative Plan revision persists the v3 message before context`
3. `initial v1 Plan uses the recoverable strict message job before writing context`

这些失败均属于 M00-B 锁定的 `web/**` Plan 合同路径，不由 M00-A.1 引入。开发者已明确授权 M00-A.1 记录该既存基线问题后继续；A 线未修复、改写或规避这些测试。

## 审核与安全

- 实现 Codex 自审：变更只覆盖四类旧流程不变量，没有生产改动或跨线文件。
- 独立只读 reviewer：`/root/m00_a1_reviewer`。首轮结论为 `With fixes`；指出 pending Store 测试名称过度声称“不重复 start”、人工确认缺少可单独绿色的真实交互门禁，以及状态/报告存在过期或未覆盖 untracked 文件的表述。所有意见修正后最终结论为 `Ready to merge=Yes`，无未解决 Critical/Important/Minor。
- 未运行完整前端 lint/build：本切片没有前端变更且 `web/**` 属于 M00-B 锁定路径；改用只读定向/边界测试验证相关旧语义。
- 未调用真实图片、视频、PPT、剪映、LLM 或其他付费 API；全部测试使用本地 DTO、内存 store、进程内 job registry 和既有纯函数。
