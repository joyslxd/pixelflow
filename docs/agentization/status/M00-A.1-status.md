# M00-A.1 切片状态

- phase：`merged`
- development line：`A`
- module：`M00-A`
- slice：`M00-A.1`
- execution mode：固定 `sequential`
- owner/Codex task：`Codex /root`（完成后写锁已释放）
- base Agent SHA：`8e626ae232d984f14fa9954b672b4e025894d426`
- previous slice commit SHA：`—`
- required freeze/dependency SHA：`8e626ae232d984f14fa9954b672b4e025894d426`
- branch：`codex/agent-0.8.4-m00-a`
- worktree absolute path：`E:\IntelliJIDEA\secondWorkSpaces\cmyqCode\pixelflow-worktrees\m00-a`
- locked files（执行期间）：`backend/tests/test_agent_runtime_legacy_invariants.py`、`docs/agentization/status/M00-A-status.md`、本文件、`docs/agentization/test-reports/M00-A.1.md`
- started at：`2026-07-22T22:25:02+08:00`
- updated at：`2026-07-22T22:53:19+08:00`

## 允许修改

- `backend/tests/test_agent_runtime_legacy_invariants.py`
- `docs/agentization/status/M00-A-status.md`
- `docs/agentization/status/M00-A.1-status.md`
- `docs/agentization/test-reports/M00-A.1.md`

## 禁止修改

- `web/**`、`docs/agentization/status/M00-B-status.md` 等 M00-B 锁定路径。
- `backend/pixelflow/agent_runtime/contracts/**`、`backend/pixelflow/agent_runtime/ports.py` 和规范 fixture；这些属于后续 M00-A.2。
- `scripts/agentization/**`；这些属于后续 M00-A.3。
- `feature/dev_0.8.4_boguan`、`feature/agent_0.8.4_boguan` 两个长期分支。

## TDD 与实现记录

- 失败测试命令/证据：RED：`python -m pytest tests/test_agent_runtime_legacy_invariants.py -q` 在测试文件创建前以“file or directory not found”失败；基线边界：`node --test tests/mainFlowContract.test.mjs` 在原 Agent worktree 与 M00-A worktree 均为 `52 passed / 3 failed`，失败均位于未改动的 Plan 前端合同。
- 最小实现：只新增旧流程 characterization tests，不修改生产代码。
- 通过测试命令/证据：新增 characterization 文件 `4 passed`；新增 + 既有后端定向 + OpenAPI 共 `15 passed`；真实确认筛选 `3 passed`；恢复不重复启动筛选 `3 passed`；确认窗口 `3 passed`；交付看板边界 `11 passed`；ruff 与 whitespace 检查通过。
- 独立 reviewer：`/root/m00_a1_reviewer`，只读审核，最终结论 `Ready to merge=Yes`，无未解决 Critical/Important/Minor。
- review 发现与处理：收窄 pending Store 测试名称，补入 PPT pending 元数据；增加真实确认与恢复不重复启动的按名称筛选门禁；修正过期状态、untracked whitespace 证据和单文件门禁措辞。

## Git 交付

- commit SHA：本状态文件所在独立提交；避免在提交内容中记录自引用哈希，最终 SHA 以远端 ref 为准。
- pushed remote branch：`origin/codex/agent-0.8.4-m00-a`
- module branch push 状态：本切片独立提交推送完成后由最终交接以 `git ls-remote` 验证。
- 是否检测到同分支/同 worktree 的其他写入者：`no`

## 下一步

- 第一条恢复命令：`git -C E:\IntelliJIDEA\secondWorkSpaces\cmyqCode\pixelflow-worktrees\m00-a status --short --branch`，然后只认领并执行 `M00-A.2`。
- 尚未运行测试及原因：未运行完整前端 lint/build，因为本切片无前端变更且 `web/**` 是 M00-B 锁定路径；相关语义已由只读定向/边界门禁验证。未运行任何真实付费 API。
- 硬阻塞：无。开发者已明确允许将基线提交 `8e626ae232d984f14fa9954b672b4e025894d426` 上 `web/tests/mainFlowContract.test.mjs` 的 3 个 M00-B/Web 既存失败记入测试报告后继续；A 线不修复或改写 `web/**`。

禁止记录凭据、Authorization、完整供应商 URL 查询参数、用户原始长 prompt 或隐藏推理。
